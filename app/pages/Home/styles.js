import {
    StyleSheet
} from 'react-native';

export default StyleSheet.create({
    homeText: {
        fontSize: 20,
        textAlign: 'center'
    },
    container: {
        flex: 1,
        padding: 10,
        justifyContent: 'center',
        alignItems: 'center',
    },
    version: {
        fontSize: 35,
        color: 'red',
    }
});